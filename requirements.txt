# Phase 0-1. Kept minimal on purpose: every dependency is a supply-chain surface, and this
# system signs commitments. Added only when a phase actually needs it.
psycopg[binary]>=3.2

# Phase 4. The always-on inbound webhook (§12). Outbound send and inbound parsing stay on the
# standard library (see concierge/postmark.py) — these two are only the HTTP server that must be
# reachable 24/7 on the VPS. uvicorn is the ASGI runner systemd supervises.
fastapi>=0.110
uvicorn[standard]>=0.29

# Feature 3 (public receipt verification, GATE 6b). Only used by the harness's
# fastapi.testclient.TestClient to drive the real /r/{receipt_id} route in-process — the route
# itself makes no outbound HTTP calls of its own.
httpx>=0.27

# Phase 6. Receipt anchoring on X Layer mainnet (concierge/xlayer.py) needs secp256k1 signing and
# RLP transaction encoding. Hand-rolling either to avoid a dependency is a worse security
# trade-off than taking one audited, widely-used library — the RPC transport itself stays on
# urllib, same as postmark.py and calcom.py.
eth_account>=0.13
